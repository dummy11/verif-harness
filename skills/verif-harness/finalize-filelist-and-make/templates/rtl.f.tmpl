# <verif_root>/filelist/rtl.f
# DUT source list. Paths are project-root-relative because the generated
# Makefile invokes the simulator from $(PROJ_ROOT).

# >>> GENERATED_RTL_SOURCES <<<
# One line per RTL file discovered under <rtl_root>, minus explicit excludes.
# Keep dependency order explicit when the DUT uses packages or interfaces.
# Neutral example:
#   rtl/demo_fifo_pkg.sv
#   rtl/demo_fifo.sv
