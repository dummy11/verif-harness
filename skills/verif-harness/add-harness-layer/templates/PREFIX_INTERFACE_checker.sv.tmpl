// <verif_root>/testbench/top/sva/<PREFIX>_<CHECKER_NAME>_checker.sv
// External protocol checker for <PREFIX>_<INTERFACE_NAME>.
// Bound by <PREFIX>_dut_harness via type-level bind (see references/stage1-patterns.md § 5).
// Portless empty stub. A VCHK action may add assertion bodies here — since
// bind places the checker inside the interface's scope, all interface signals are
// reachable by name without port passing.

module <PREFIX>_<CHECKER_NAME>_checker ();
  // VCHK assertion bodies (`default disable iff (!rst_n)` recommended).
endmodule
