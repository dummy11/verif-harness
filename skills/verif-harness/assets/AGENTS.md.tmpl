# {{PROJECT_NAME}} RTL Verification Agent Instructions

本仓库采用 OpenAI harness-style 的 AI 辅助开发流程。

`verif-harness` 是最上层控制面。新项目的可编辑规格统一位于 Spec Kit
`specs/`；`{{DOCS_ROOT}}` 保存治理、规格派生视图、证据索引和 review packet，
不得成为第二个可编辑 requirements authority。追踪链使用
`REQ -> VF -> PLAN -> TASK -> MODE -> ARTIFACT -> EVIDENCE -> GATE`。

## 仓库范围

- `{{RTL_ROOT}}`：DUT 源码（`{{DUT_TOP_FILE}}` 为顶层，module 名
  `{{DUT_TOP_MODULE}}`）。**agent 不得修改 `{{RTL_ROOT}}`**，只读。
- `{{VERIF_ROOT}}`：验证工作根目录，所有验证代码、文档、脚本都放这里。
- `.harness/`：harness 骨架（检查脚本、review-block 参考）。
- `.harness-config.json`：本项目 harness 配置，脚本从这里读路径与参数。
- `.codex/`：Codex 项目配置与自定义 agents。

## 必读上下文

在修改仓库前，agent 必须先读取：

- `{{WORKFLOW_PATH}}`
- `{{PLAN_PATH}}`
- `{{ROADMAP_PATH}}`
- `{{METHODOLOGY_PATH}}`
- `{{VERIF_PLAN_PATH}}`
- `{{TB_ARCHITECTURE_PATH}}`
- `{{COVERAGE_PLAN_PATH}}`
- `{{ASSERTION_PLAN_PATH}}`
- `{{TESTCASE_LIST_PATH}}`
- `{{FEATURE_MATRIX_PATH}}`
{{#IF_REFMODEL}}- `{{REFMODEL_SPEC_LOCAL_PATH}}`
{{/IF_REFMODEL}}

修改 Markdown 文档前，还必须读取：

- `.harness/check_ai_workflow.py`

修改 TB 代码前，还必须读取：

- 对应 stage 在 `{{ROADMAP_PATH}}` 中的目标与范围。
- `{{TB_ARCHITECTURE_PATH}}` 中受影响组件的定义。

{{#IF_DESIGN_DOCS}}## RTL 设计文档

RTL 侧设计文档位于 `{{DESIGN_DOCS_ROOT}}`，作为 DUT spec 的补充上下文。修改验证策略前如涉及 RTL 行为解释，先读该目录。

{{/IF_DESIGN_DOCS}}## 工作原则

- 文档优先，代码后置。
- Stage 0 只建立文档和 workflow 基线。
- Stage 0 不实现 TB 代码。
- Stage 0 文档集作为一个整体 review，不按每个文档拆独立 review 阶段。
- Stage 1 起每个 stage 都产出真实可运行代码，范围逐层扩展，不允许"前面几 stage 全是空壳"。
- 不得修改 `{{RTL_ROOT}}` 目录。DUT 是外部资产。
- 规格不明确时必须记录 open question，不能在代码中静默假设。
- 不确定假设必须交给 human review，不能由 agent 自行下结论。
- driver / monitor / sequencer / agent / env / scoreboard / reference model 必须保持分层。
- 新增可验证 feature 必须更新 `{{FEATURE_MATRIX_PATH}}`。
- 新增 testcase 必须更新 `{{TESTCASE_LIST_PATH}}`。
- 新增 coverage bin 必须更新 `{{COVERAGE_PLAN_PATH}}`。
- 新增 assertion 必须更新 `{{ASSERTION_PLAN_PATH}}`。
{{#IF_REFMODEL}}- Reference model 语义变更必须同步
  `{{REFMODEL_SPEC_LOCAL_PATH}}`，并核对上游
  `{{REFMODEL_UPSTREAM_PATH}}`。
{{/IF_REFMODEL}}

## Markdown Workflow

生成或修改 Markdown 文件后，必须运行：

```bash
python3 .harness/check_ai_workflow.py --fix
```

如果脚本修改了文件，agent 必须重新 review 相关 diff。

## Stage 入口

进入某个 stage 前，agent 必须确认：

- 当前 stage 的目标在 `{{ROADMAP_PATH}}` 中存在。
- 当前 stage 的范围在 `{{VERIF_PLAN_PATH}}` 中有定义。
- 当前 stage 不违反 `{{WORKFLOW_PATH}}`。
- 当前 stage 不违反 `{{METHODOLOGY_PATH}}`。

## 禁止事项

- 不要在 Stage 0 添加 TB 代码。
- 不要修改 `{{RTL_ROOT}}` 下任何文件。
- 不要跳过 `.harness/check_ai_workflow.py --fix`。
- 不要绕过 `{{TB_ARCHITECTURE_PATH}}` 修改组件分层。
- 不要在没有 change request 的情况下修改 frozen Human Decisions。
- 不要静默覆盖人工 review 决策。
- 不要把待确认假设写成已经批准的结论。
{{#IF_REFMODEL}}- 不要在 reference model 语义与上游不一致时静默 diverge。
{{/IF_REFMODEL}}

## Lifecycle

Status: Draft
Baseline: Pre-Stage 0
Frozen Sections:

- None

Mutable Sections:

- All

## Review Trace

### Review Pending

Reviewer: Human
Status: Pending

Findings:

- Initial AGENTS.md draft awaiting Stage 0 baseline review.

Required Changes:

- Pending human review.

Resolution:

- Pending human review.

## Revision Log

- {{DATE_TODAY}}: Initial AGENTS.md generated by verif-harness skill.

## File-Level Human Review

### Project-Level References

- `{{WORKFLOW_PATH}}`
- `{{PLAN_PATH}}`
- `{{ROADMAP_PATH}}`
- `{{METHODOLOGY_PATH}}`
- `{{VERIF_PLAN_PATH}}`
- `{{TB_ARCHITECTURE_PATH}}`
- `{{COVERAGE_PLAN_PATH}}`
- `{{ASSERTION_PLAN_PATH}}`
- `{{TESTCASE_LIST_PATH}}`
- `{{FEATURE_MATRIX_PATH}}`
{{#IF_REFMODEL}}- `{{REFMODEL_SPEC_LOCAL_PATH}}`
{{/IF_REFMODEL}}

### Review Metadata

- Status: Pending
- Reviewer: Human
- Decision Date: TBD

### Human Decisions

- Pending human review.

### Human Review Notes

- None

### Approval Decision

Pending human review.
