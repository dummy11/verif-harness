interface:
  display_name: "Verification Harness"
  short_description: "Govern spec-driven RTL verification from Stage 0 to freeze"
  default_prompt: "Use $verif-harness as the top-level control plane, manage specifications through pinned GitHub Spec Kit, dispatch reviewed tasks to the appropriate verification mode, and keep xverif, WavePeek, EDA evidence, and Human approval boundaries explicit."
