interface:
  display_name: "Verification Harness"
  short_description: "Govern continuous RTL verification from VPlan to closure"
  default_prompt: "Use $verif-harness as the v1 control plane: design re-entrant Workstream desired state with VPlan, query VModel read-only, record facts through structured ingress, reconcile automatically with VCheck/VClosure, use VReason only for ambiguity, and keep DUT, evidence, and Human approval boundaries explicit."
