# verif-harness RTL bundle

这是待发布的 Spec Kit bundle manifest，组合 RTL verification preset 与 Stage
lifecycle workflow。它在进入受信任 Spec Kit catalog 前不作为远程可安装组件；
本仓库开发和验证使用 `../preset/rtl-verification` 与
`../workflows/verif-stage-lifecycle.yml` 的本地路径。

bundle 安装成功不代表任何 RTL 验证结果或审批。
